<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= categoryName %> 카테고리</title>
  <link rel="stylesheet" href="/public/css/categories/detail_category.css">
</head>

<body>
  <%- include("../common/header") %>
  <%- include("../common/side_bar") %>
  <%- include("../common/footer") %>

  <main>
    <div class="top-bar">
      <h2 class="title">
        <%= categoryName ? categoryName + " 카테고리" : "전체 동호회 목록" %>
      </h2>
      <form class="recommend-search-bar" method="GET" action="">
        <input
          type="text"
          name="keyword"
          placeholder="동호회 이름 검색"
          value="<%= keyword || '' %>"
        />
        <button type="submit">검색</button>
      </form>
    </div>
  
    <!-- 세부 카테고리 바 -->
  <div class="subcategory-bar">
    <a href="/categories/<%= categoryName %>?subCategory=전체"
        class="<%= selectedSubCategory === '전체' ? 'selected' : '' %>">
      전체
    </a>
    <% subCategories.forEach(sub => { %>
      <a href="/categories/<%= categoryName %>?subCategory=<%= sub %>"
          class="<%= selectedSubCategory === sub ? 'selected' : '' %>">
        <%= sub %>
      </a>
    <% }) %>
  </div>
  
    <!-- ✅ club-container -->
    <div class="club-container">
      <div class="club-cards">
        <% clubs.forEach(club => { %>
          <div class="club-card">
            <img src="<%= club.image %>" alt="<%= club.name %>" class="club-img">
            <div class="club-info">
              <h3><%= club.name %></h3>
              <p>카테고리: <%= club.club_category_name %></p>
              <p>소개: <%= club.introduction || "지역 정보 없음" %></p>
              <p>멤버 수: <%= club.member_limit || 0 %>명</p>
            </div>
          </div>
        <% }) %>
      </div>
  
      <!-- ✅ 추천 섹션 -->
      <div class="recommend-section">
        <h3>게스트 체험이 있는 동호회 추천</h3>
        <% recommendedClubs.forEach(club => { %>
          <div class="recommend-card">
            <img src="<%= club.image %>" alt="<%= club.name %>" />
            <p><%= club.name %></p>
          </div>
        <% }) %>
      </div>
    </div>
  </main>
</body>
</html>
