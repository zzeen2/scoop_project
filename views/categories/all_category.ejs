<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <link rel="stylesheet" href="/public/css/categories/all_category.css">
</head>

<body>
    <%- include("../common/header") %>
    <%- include("../common/side_bar") %>
    <%- include("../common/footer") %>
      
    <main>
      <div class="top-bar">
        <h2 class="title">
          <%= categoryName ? categoryName + " 카테고리" : "전체 동호회 목록" %>
        </h2>
        <form class="recommend-search-bar" method="GET" action="">
          <input
            type="text"
            name="keyword"
            placeholder="동호회 이름 검색"
            value="<%= keyword || '' %>"
          />
          <button type="submit">검색</button>
        </form>
      </div>
    
      <div class="club-container">
        <div class="club-cards">
          <% clubs.forEach(club => { %>
            <a href="/clubs/detail/<%= club.club_id %>" class="club-card-link">
              <div class="club-card">
                <img src="<%= club.image %>" alt="동호회 이미지" class="club-img">
                <div class="club-info">
                  <h3><%= club.name %></h3>
                  <p>카테고리: <%= club.club_category_name %></p>
                  <p>소개: <%= club.introduction ? club.introduction : '정보 없음' %></p>
                  <p>멤버 수: <%= club.member_limit ? club.member_limit : 0 %>명</p>
                </div>
              </div>
            </a>
          <% }) %>
        </div>
      </div>
      
    
        <div class="recommend-section">
          <h3>게스트 체험이 있는 동호회 추천</h3>
          <% recommendedClubs.forEach(club=> { %>
            <div class="recommend-card">
              <img src="<%= club.image %>" alt="<%= club.name %>" />
              <p><%= club.name %></p>
            </div>
          <% }) %>
        </div>
      </div>
    </main>
    <script src = "../../public/script/categories/all_category.js"></script>
</html>